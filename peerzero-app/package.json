{
  "name": "peerzero-app",
  "version": "1.0.0",
  "private": true,
  "description": "PeerZero Bot Marketplace — System 2 (separate from the School backend)",
  "packageManager": "pnpm@10.29.3",
  "pnpm": {
    "overrides": {
      "glob@>=11.0.0 <11.1.0": ">=11.1.0",
      "tar@<7.5.11": ">=7.5.11",
      "picomatch@<2.3.2": ">=2.3.2",
      "picomatch@>=3.0.0 <3.0.2": ">=3.0.2",
      "picomatch@>=4.0.0 <4.0.4": ">=4.0.4",
      "@expo/cli>picomatch": ">=4.0.4",
      "node-forge@<1.4.0": ">=1.4.0"
    },
    "//auditNotes": {
      "esbuild@0.21.5 (moderate)": "GHSA-67mh-4wv8-2f99 — dev server CORS bypass. Dev-only (vitest>vite>esbuild), not a production risk. Cannot override to >=0.25.0 without breaking vite/vitest (breaking changes). Will resolve when vite upgrades its esbuild dep. Check: pnpm audit",
      "node-forge@1.3.3 (high)": "GHSA-5m6q-g25r-mvwx, GHSA-2328-f5f3-gj25, GHSA-q67f-28xg-22rw, GHSA-ppp5-5v6c-4jwp — DoS, cert bypass, Ed25519+RSA signature forgery. Comes from @expo/cli>@expo/code-signing-certificates>node-forge and @expo/cli>node-forge. Override to >=1.4.0."
    }
  },
  "workspaces": [
    "packages/shared",
    "packages/server",
    "packages/mobile"
  ],
  "scripts": {
    "server:dev": "cd packages/server && npm run dev",
    "server:build": "cd packages/server && npm run build",
    "mobile:start": "cd packages/mobile && npx expo start",
    "db:migrate": "cd packages/server && npm run migrate",
    "db:migrate:down": "cd packages/server && npm run migrate:down",
    "db:migrate:create": "cd packages/server && npm run migrate:create",
    "db:seed": "cd packages/server && npx tsx src/db/seed-products.ts",
    "test": "cd packages/server && npm test"
  }
}

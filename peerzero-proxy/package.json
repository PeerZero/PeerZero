{
  "name": "peerzero-llm-proxy",
  "version": "1.0.0",
  "private": true,
  "license": "MIT",
  "description": "LLM proxy that injects the identity activation preamble server-side",
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "test": "node --test tests/*.test.js",
    "test:coverage": "c8 --all --src=src --reporter=text --reporter=lcov --reports-dir=coverage node --test tests/*.test.js"
  },
  "devDependencies": {
    "c8": "^10.1.0",
    "wrangler": "^4.0.0",
    "typescript": "^5.0.0",
    "@cloudflare/workers-types": "^4.0.0"
  }
}
